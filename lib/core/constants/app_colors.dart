import 'package:flutter/material.dart';

class AppColors {
  // Main ALU_HUB colors
  static const Color primaryBlue = Color(0xFF27377D);
  static const Color darkBlue = Color(0xFF1F2D68);
  static const Color accentBlue = Color(0xFF5367D8);

  // Background colors
  static const Color lightBackground = Color(0xFFF5F6FA);
  static const Color darkBackground = Color(0xFF11172B);

  // Card colors
  static const Color lightCard = Colors.white;
  static const Color darkCard = Color(0xFF1B2545);

  // Text colors
  static const Color darkText = Color(0xFF1B1D2A);
  static const Color lightText = Colors.white;
  static const Color mutedText = Color(0xFF70758A);

  // Prevent creating this class as an object
  const AppColors._();
}
